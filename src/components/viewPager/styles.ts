import {createStyleSheet} from 'react-native-unistyles';

export const stylesheet = createStyleSheet(theme => ({
  container: {
    flex: 1,
  },
  newFeedView: {
    backgroundColor: '#fff',
    marginTop: 8,
    marginBottom: 100,
  },
}));
